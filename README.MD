# Notes:

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     add the custom base to multiply             [number] [required]
  -l, --list     add feature to show generated table  [boolean] [default: false]
  -u, --until    add custom multiplier limit              [number] [default: 10]
```